version: '3'

services:
  elf:
    image: ghcr.io/muncomputersciencesociety/elf
    restart: always
    environment:
      GIN_MODE: release
    env_file:
      - .env
    volumes:
      - ./elf.sqlite:/elf/elf.sqlite
